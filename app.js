// var h1 = document.createElement('h1');

// h1.innerText = "Hello for JS";

// document.body.appendChild(h1);

let a = 29;

// this is the default export (it export particular thing)
export default a;

// name export
export const arr = [1, 2, 3, 4, 4];





























































































































